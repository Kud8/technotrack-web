{% extends "base.html" %}

{% block title %}Список постов{% endblock %}

{% block content %}
{% for post in object_list %}
    <div>
        <div><i>{{ post.pub_date }}</i></div>
        <div><a href = "{{ post.post_id }}"><b>{{ post.title }}</b></a></div>
        <a>{{ post.text }}</a>
        <br>
        <br>
    </div>
{% endfor %}
{% endblock %}

